const validateEmail = require('./validateEmail');
const urlToBase64 = require('./urlToBase64');
const constantDate = require('./constantDate');

module.exports = {
  validateEmail,
  urlToBase64,
  constantDate
}
